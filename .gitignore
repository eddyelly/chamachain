# dependencies
node_modules/
web/node_modules/

# env (never commit real keys)
.env
.env.local
web/.env
web/.env.local

# hardhat
artifacts/
cache/
typechain-types/
types/

# deployment records (chain-specific, regenerated by the deploy script)
deployments/

# next.js
web/.next/
web/out/
web/build/

# misc
.DS_Store
*.log
npm-debug.log*
coverage/

# local workflow files (kept on disk, not published)
.superpowers/
docs/
CLAUDE.md
